# Question 61

## Question
You have two Azure subscriptions named `Sub1` and `Sub2`.

An administrator creates a custom role that has an assignable scope to a resource group named `RG1` in `Sub1`.

You need to ensure that you can apply the custom role to any resource group in `Sub1` and `Sub2`. The solution must minimize administrative effort.

What should you do?

- A. Select the custom role and add Sub1 and Sub2 to the assignable scopes. Remove RG1 from the assignable scopes.  
- B. Create a new custom role for Sub1. Create a new custom role for Sub2. Remove the role from RG1.  
- C. Create a new custom role for Sub1 and add Sub2 to the assignable scopes. Remove the role from RG1.  
- D. Select the custom role and add Sub1 to the assignable scopes. Remove RG1 from the assignable scopes. Create a new custom role for Sub2.  

---

## Correct Answer
**A. Select the custom role and add Sub1 and Sub2 to the assignable scopes. Remove RG1 from the assignable scopes.**

---

## Explanation

Custom RBAC roles use:
```json
"assignableScopes"
```

to define where the role can be assigned.

Currently, the role is limited to:
```text
RG1 in Sub1
```

To allow the role to be assigned to:
- Any resource group in Sub1
- Any resource group in Sub2

you should expand the assignable scopes to:
```json
"assignableScopes": [
  "/subscriptions/Sub1",
  "/subscriptions/Sub2"
]
```

and remove the RG1-specific scope.

This:
- Enables use across both subscriptions
- Avoids creating duplicate roles
- Minimizes administrative effort

---

## Example

### Current Scope
```json
"assignableScopes": [
  "/subscriptions/Sub1/resourceGroups/RG1"
]
```

### Correct Updated Scope
```json id="az104-custom-role"
"assignableScopes": [
  "/subscriptions/Sub1",
  "/subscriptions/Sub2"
]
```

---

## Why the Other Options Are Incorrect

### B. Create separate custom roles
Incorrect.

Creates unnecessary duplicate roles and increases administration.

---

### C. Create a new role for Sub1
Incorrect.

Still creates unnecessary duplication.

---

### D. Create another role for Sub2
Incorrect.

More administrative effort than modifying the existing role.

---

# Question 62

## Question
You have an Azure Subscription that contains a storage account named `storageacct1234` and two users named `User1` and `User2`.

You assign User1 the following roles:

| Role | Scope | Group Assignment | Condition |
|---|---|---|---|
| Reader | Resource Group (inherited) | --- | None |
| Storage Blob Data Contributor | This Resource | --- | Add |

Which two actions can User1 perform?

- A. Assign roles to User2 for storageacct1234.  
- B. Upload blob data to storageacct1234.  
- C. Modify the firewall of storageacct1234.  
- D. View blob data in storageacct1234.  
- E. View file shares in storageacct1234.  

---

## Correct Answers
- **B. Upload blob data to storageacct1234**
- **D. View blob data in storageacct1234**

---

## Explanation

### ✔ Reader Role
The Reader role allows:
- Viewing Azure resource configuration
- Viewing storage account settings

But it does NOT allow:
- Modifying resources
- Uploading data
- Managing RBAC

---

### ✔ Storage Blob Data Contributor Role
The Storage Blob Data Contributor role allows:
- Read blob data
- Upload blob data
- Modify blob data
- Delete blob data

This role applies specifically to:
- Blob containers and blob data

---

## Why the Correct Answers Are Correct

### B. Upload blob data
Correct.

Storage Blob Data Contributor includes:
```text
Microsoft.Storage/storageAccounts/blobServices/containers/blobs/write
```

So User1 can upload blobs.

---

### D. View blob data
Correct.

The role also allows reading blob data.

---

## Why the Other Options Are Incorrect

### A. Assign roles to User2
Incorrect.

Assigning roles requires:
- Owner
or
- User Access Administrator

User1 does not have either role.

---

### C. Modify the firewall
Incorrect.

Firewall configuration requires management-plane permissions such as:
- Contributor
- Owner

Reader cannot modify settings.

---

### E. View file shares
Incorrect.

Storage Blob Data Contributor applies only to:
- Blob storage

It does NOT grant access to:
- Azure Files

---

# Question 63

## Question
You have an Azure subscription named `Subscription1` that contains an Azure Log Analytics workspace named `Workspace1`.

You need to view the error events from a table named `Event`.

Which query should you run in Workspace1?

- A. `select * from Event where EventType == "error"`  
- B. `Event | search "error"`  
- C. `Event | where EventType is "error"`  
- D. `Get-Event Event | where {$_.EventType == "error"}`  

---

## Correct Answer
**B. `Event | search "error"`**

---

## Explanation

Azure Log Analytics uses:
- **Kusto Query Language (KQL)**

The correct KQL syntax to search a table for the term `"error"` is:

```kusto
Event
| search "error"
```

This searches all columns in the `Event` table for the word:
```text
error
```

---

## Why the Other Options Are Incorrect

### A. SQL syntax
```sql
select * from Event where EventType == "error"
```

Incorrect.

Azure Log Analytics does NOT use SQL syntax.

---

### C. Invalid KQL syntax
```kusto
Event | where EventType is "error"
```

Incorrect.

KQL uses:
```kusto
==
```

not:
```kusto
is
```

Correct syntax would be:
```kusto
Event
| where EventType == "error"
```

---

### D. PowerShell syntax
```powershell
Get-Event Event | where {$_.EventType == "error"}
```

Incorrect.

This is PowerShell syntax, not KQL.

---

## Correct Query Example

```kusto
Event
| search "error"
```

---

# Question 65

## Question
You have an Azure Active Directory (Azure AD) tenant named `contoso.com`.

You have a CSV file that contains the names and email addresses of 500 external users.

You need to create a guest user account in `contoso.com` for each of the 500 external users.

Solution: From Azure AD in the Azure portal, you use the **Bulk invite users** operation.

Does this meet the goal?

- A. Yes  
- B. No  

---

## Correct Answer
**A. Yes**

---

## Explanation

Azure AD provides a feature called:
```text
Bulk invite users
```

This feature allows administrators to:
- Import external users from a CSV file
- Create guest accounts in Azure AD
- Send invitation emails automatically

The CSV file typically includes:
- Name
- Email address

This is the recommended method for inviting large numbers of guest users efficiently.

---

## Why the Solution Works

Using:
```text
Azure AD → Users → Bulk operations → Bulk invite
```

supports:
- Guest user creation
- CSV-based import
- Bulk invitation processing

Therefore, the solution meets the goal.

---

## Final Answer
**A. Yes**

---

# Question 66

## Question
You have an Azure subscription that is linked to an Azure AD tenant.

The tenant contains the custom role-based access control (RBAC) roles shown in the following table.

| Name | Description |
|---|---|
| Role1 | Azure Subscription Role |
| Role2 | Azure AD Role |

From the Azure portal, you need to create two custom roles named Role3 and Role4.

- Role3 will be an Azure subscription role.
- Role4 will be an Azure AD role.

Which roles can you clone to create the new roles?

---

## Answer Area

| New Role | Role to Clone |
|---|---|
| Role3 | Role1 |
| Role4 | Role2 |

---

## Explanation

Azure has two separate role systems:

### ✔ Azure RBAC Roles
Used for:
- Azure subscriptions
- Resource groups
- Azure resources

These roles can only be cloned from:
- Other Azure RBAC roles

Therefore:
- Role3 (Azure subscription role) → clone from Role1

---

### ✔ Azure AD Roles
Used for:
- Azure Active Directory management
- Users, groups, authentication, directory operations

These roles can only be cloned from:
- Other Azure AD roles

Therefore:
- Role4 (Azure AD role) → clone from Role2

---

## Final Answers

| Role | Clone From |
|---|---|
| Role3 | Role1 |
| Role4 | Role2 |

---

# Question 68

## Question
You have an Azure subscription that contains:
- 10 virtual machines
- A key vault named `Vault1`
- A network security group named `NSG1`

All resources are deployed to the East US Azure region.

The virtual machines are protected by using `NSG1`.

`NSG1` is configured to block all outbound traffic to the internet.

You need to ensure that the virtual machines can access `Vault1`.

The solution must:
- Use the principle of least privilege
- Minimize administrative effort

What should you configure as the destination of the outbound security rule for NSG1?

- A. an application security group  
- B. a service tag  
- C. an IP address range  

---

## Correct Answer
**B. a service tag**

---

## Explanation

Azure provides **service tags** for Azure services such as:
- AzureKeyVault
- Storage
- SQL
- AzureMonitor

A service tag:
- Represents a group of IP address prefixes managed by Azure
- Automatically updates when Azure changes service IP ranges
- Minimizes administration effort

For Azure Key Vault access, use the service tag:
```text
AzureKeyVault
```

This allows the VMs to access Vault1 without manually maintaining IP addresses.

---

## Why the Other Options Are Incorrect

### A. Application security group
Incorrect.

Application Security Groups (ASGs) organize VM network interfaces, not Azure platform services like Key Vault.

---

### C. IP address range
Incorrect.

Possible, but:
- Requires manual maintenance
- IP ranges can change
- Higher administrative effort

❌ Does not meet the requirement to minimize administrative effort.

---

## Example NSG Rule

```text
Source: VirtualNetwork
Destination: AzureKeyVault
Protocol: TCP
Port: 443
Action: Allow
```

---

## Final Answer
**B. a service tag**

---

# Question 69

## Question
You have an Azure AD tenant named `adatum.com` that contains the groups shown below.

### Groups

| Name | Member of |
|---|---|
| Group1 | None |
| Group2 | Group1 |
| Group3 | Group2 |

---

### Users

| Name | Member of |
|---|---|
| User1 | Group1 |
| User2 | Group2 |
| User3 | Group3 |
| User4 | None |

You assign the **Azure Active Directory Premium Plan 2** license to:
- Group1
- User4

Which users are assigned the license?

- A. User4 only  
- B. User1 and User4 only  
- C. User1, User2, and User4 only  
- D. User1, User2, User3, and User4  

---

## Correct Answer
**D. User1, User2, User3, and User4**

---

## Explanation

This question tests **group-based license assignment with nested groups**.

### ✔ Key Concept: Nested group licensing
When a license is assigned to a group:
- All direct members get the license
- All members of nested groups also inherit it

---

### Step-by-step breakdown

#### 1. Group1 gets the license
- User1 is directly in Group1 → ✔ licensed

Also:
- Group2 is a member of Group1 → inherited license
- Group3 is inside Group2 → also inherited

---

#### 2. License inheritance chain

- Group1 → contains User1 + Group2
- Group2 → contains User2 + Group3
- Group3 → contains User3

So:
- User1 → licensed
- User2 → licensed (via Group2 → Group1)
- User3 → licensed (via Group3 → Group2 → Group1)

---

#### 3. User4 is directly assigned
- User4 → explicitly assigned license

---

## Final Result

| User | Licensed |
|---|---|
| User1 | Yes |
| User2 | Yes |
| User3 | Yes |
| User4 | Yes |

---

## Final Answer
**D. User1, User2, User3, and User4**

---

# Question 71

## Question
You have an Azure subscription named `Subscription1` that contains a virtual network named `VNet1`. `VNet1` is in a resource group named `RG1`.

Subscription1 has a user named `User1`.

User1 has the following roles:
- Reader
- Security Admin
- Security Reader

You need to ensure that User1 can assign the Reader role for `VNet1` to other users.

What should you do?

- A. Assign User1 the Network Contributor role for VNet1.  
- B. Remove User1 from the Security Reader role for Subscription1. Assign User1 the Contributor role for RG1.  
- C. Assign User1 the Owner role for VNet1.  
- D. Assign User1 the Network Contributor role for RG1.  

---

## Correct Answer
**C. Assign User1 the Owner role for VNet1.**

---

## Explanation

To assign RBAC roles to other users, a user must have:
- **Owner**
or
- **User Access Administrator**

Among the available options, only:
```text
Owner
```

includes permission to:
```text
Microsoft.Authorization/roleAssignments/*
```

This allows User1 to assign the Reader role to other users for VNet1.

---

## Why the Other Options Are Incorrect

### A. Network Contributor for VNet1
Can manage networking resources, but:
- Cannot assign RBAC roles

---

### B. Contributor for RG1
Contributor can manage resources, but:
- Cannot manage access permissions

---

### D. Network Contributor for RG1
Still cannot assign RBAC roles.

---

## Final Answer
**C. Assign User1 the Owner role for VNet1.**

---

# Question 72

## Question
You have an Azure subscription that contains the users shown in the following table.

### Users

| Name | Member of |
|---|---|
| User1 | Group1 |
| User2 | Group2 |
| User3 | Group3 |

---

### Groups

| Name | Type | Azure AD roles can be assigned to the group |
|---|---|---|
| Group1 | Security | Yes |
| Group2 | Security | Yes |
| Group3 | Microsoft 365 | Yes |

---

### RG1 – Access Control (IAM)

| Name | Type | Role | Scope | Condition |
|---|---|---|---|---|
| Group1 | Group | Owner | This Resource | None |

---

## Statements

For each of the following statements, select Yes if the statement is true. Otherwise, select No.

| Statement | Answer |
|---|---|
| You can assign User2 the Owner role for RG1 by adding Group2 as a member of Group1. | Yes |
| You can assign User3 the Owner role for RG1 by adding Group3 as a member of Group1. | No |
| You can assign User3 the Owner role for RG1 by assigning the Owner role to Group3 for RG1. | No |

---

## Explanation

### 1. Add Group2 as member of Group1 → YES
- Group1 already has the Owner role on RG1.
- Group2 is a Security group.
- Azure RBAC supports nested Security groups.

Therefore:
- User2 inherits Owner permissions through Group2 → Group1.

---

### 2. Add Group3 as member of Group1 → NO
- Group3 is a Microsoft 365 group.
- Microsoft 365 groups cannot be nested into Security groups for Azure RBAC inheritance.

Therefore:
- User3 will not inherit Owner access this way.

---

### 3. Assign Owner role directly to Group3 → NO
Azure RBAC role assignments support:
- Users
- Service principals
- Security groups

Microsoft 365 groups are not supported for Azure RBAC role assignments.

Therefore:
- Owner role cannot be assigned to Group3.

---

# Question 74
## Question
You have an Azure subscription that contains a user named `User1` and the resources shown in the following table.

| Name | Type |
|---|---|
| RG1 | Resource Group |
| networkinterface1 | Virtual Network Interface |
| NSG1 | Network Security Group |

`NSG1` is associated to `networkinterface1`.

User1 has the following role assignments for NSG1:

| Role | Scope |
|---|---|
| Contributor | This Resource |
| Reader | Subscription (Inherited) |
| Storage Account Contributor | Resource Group (Inherited) |

---

## Statements

For each of the following statements, select Yes if the statement is true. Otherwise, select No.

| Statement | Answer |
|---|---|
| User1 can create a storage account in RG1. | Yes |
| User1 can modify the DNS settings of networkinterface1. | No |
| User1 can create an inbound security rule to filter inbound traffic to networkinterface1. | Yes |

---

## Explanation

### 1. Create a storage account in RG1 → YES
User1 has:
```text
Storage Account Contributor
```
at the Resource Group scope.

This role allows:
- Creating
- Managing
- Configuring storage accounts

within RG1.

---

### 2. Modify DNS settings of networkinterface1 → NO
User1 has Contributor only on:
```text
NSG1
```

not on:
```text
networkinterface1
```

Therefore, User1 cannot modify NIC DNS settings.

---

### 3. Create inbound security rule on NSG1 → YES
Contributor role on NSG1 allows:
- Creating NSG rules
- Modifying NSG rules
- Deleting NSG rules

Since NSG1 is attached to networkinterface1, User1 can create inbound filtering rules.

---

# Question 75

## Question
You have three Azure subscriptions named `Sub1`, `Sub2`, and `Sub3` that are linked to an Azure AD tenant.

The tenant contains:
- A user named `User1`
- A security group named `Group1`
- A management group named `MG1`

User1 is a member of Group1.

- Sub1 and Sub2 are members of MG1.
- Sub1 contains a resource group named RG1.
- RG1 contains five Azure Functions.

You create the following role assignments for MG1:
- Group1: Reader
- User1: User Access Administrator

You assign User1 the Virtual Machine Contributor role for Sub1 and Sub2.

---

## Statements

For each of the following statements, select Yes if the statement is true. Otherwise, select No.

| Statement | Answer |
|---|---|
| The Group1 members can view the configurations of the Azure functions. | Yes |
| User1 can assign the Owner role for RG1. | Yes |
| User1 can create a new resource group and deploy a virtual machine to the new group. | No |

---

## Explanation

### 1. Group1 members can view Azure Functions configurations → YES
Group1 has:
```text
Reader
```
assigned at:
```text
MG1
```

Since:
- Sub1 and Sub2 inherit permissions from MG1
- RG1 is inside Sub1

Group1 members can view all resource configurations, including Azure Functions.

---

### 2. User1 can assign the Owner role for RG1 → YES
User1 has:
```text
User Access Administrator
```
at MG1 scope.

This allows User1 to:
- Manage RBAC role assignments
- Assign roles such as Owner

Inherited down to:
- Sub1
- RG1

---

### 3. User1 can create a new resource group and deploy a VM → NO
User1 has:
```text
Virtual Machine Contributor
```

This role allows:
- Managing virtual machines

But it does NOT allow:
- Creating resource groups

Therefore:
- User1 cannot create a new resource group.

Without the resource group, the VM deployment requirement is not fully met.

---
