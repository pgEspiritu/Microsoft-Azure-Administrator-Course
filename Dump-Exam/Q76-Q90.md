# Question 76

## Question
You have three Azure subscriptions named `Sub1`, `Sub2`, and `Sub3` that are linked to an Azure AD tenant.

The tenant contains:
- A user named `User1`
- A security group named `Group1`
- A management group named `MG1`

User1 is a member of Group1.

- Sub1 and Sub2 are members of MG1.
- Sub1 contains a resource group named RG1.
- RG1 contains five Azure Functions.

You create the following role assignments for MG1:
- Group1: Reader
- User1: User Access Administrator

You assign User1 the Virtual Machine Contributor role for Sub1 and Sub2.

---

## Statements

For each of the following statements, select Yes if the statement is true. Otherwise, select No.

| Statement | Answer |
|---|---|
| The Group1 members can view the configurations of the Azure functions. | Yes |
| User1 can assign the Owner role for RG1. | Yes |
| User1 can create a new resource group and deploy a virtual machine to the new group. | No |

---

## Explanation

### 1. Group1 members can view Azure Functions configurations → YES
Group1 has:
```text
Reader
```
assigned at:
```text
MG1
```

Since:
- Sub1 and Sub2 inherit permissions from MG1
- RG1 is inside Sub1

Group1 members can view all resource configurations, including Azure Functions.

---

### 2. User1 can assign the Owner role for RG1 → YES
User1 has:
```text
User Access Administrator
```
at MG1 scope.

This allows User1 to:
- Manage RBAC role assignments
- Assign roles such as Owner

Inherited down to:
- Sub1
- RG1

---

### 3. User1 can create a new resource group and deploy a VM → NO
User1 has:
```text
Virtual Machine Contributor
```

This role allows:
- Managing virtual machines

But it does NOT allow:
- Creating resource groups

Therefore:
- User1 cannot create a new resource group.

---

# Question 77

## Question
You have an Azure subscription named `Subscription1` that contains a virtual network named `VNet1`. `VNet1` is in a resource group named `RG1`.

Subscription1 has a user named `User1`.

User1 has the following roles:
- Reader
- Security Admin
- Security Reader

You need to ensure that User1 can assign the Reader role for `VNet1` to other users.

What should you do?

- A. Remove User1 from the Security Reader role for Subscription1. Assign User1 the Contributor role for RG1.  
- B. Assign User1 the User Access Administrator role for VNet1.  
- C. Remove User1 from the Security Reader and Reader roles for Subscription1.  
- D. Assign User1 the Contributor role for VNet1.  

---

## Correct Answer
**B. Assign User1 the User Access Administrator role for VNet1.**

---

## Explanation

To assign RBAC roles to other users, a user must have:
- Owner
or
- User Access Administrator

The:
```text
User Access Administrator
```

role specifically allows:
- Managing user access
- Creating role assignments
- Delegating RBAC permissions

without granting full resource management rights.

Assigning it at the VNet1 scope follows:
- Principle of least privilege

---

## Why the Other Options Are Incorrect

### A. Contributor for RG1
Contributor can manage resources but:
- Cannot assign RBAC roles

---

### C. Remove existing roles
Removing Reader/Security Reader does not grant role assignment permissions.

---

### D. Contributor for VNet1
Contributor cannot manage access control assignments.

---

## Final Answer
**B. Assign User1 the User Access Administrator role for VNet1.**
Without the resource group, the VM deployment requirement is not fully met.

---

# Question 77

## Question
You have an Azure AD tenant named `adatum.com` that contains the groups shown in the following table.

| Name | Type | Member Of |
|---|---|---|
| Group1 | Security | None |
| Group2 | Security | Group1 |

Adatum.com contains the users shown in the following table.

| Name | Type |
|---|---|
| User1 | Group1 |
| User2 | Group2 |

You assign an Azure Active Directory Premium P2 license to Group1 with the following assignment options:

| License Option | State |
|---|---|
| Azure Active Directory Premium P1 | Off |
| Azure Active Directory Premium P2 | On |
| Microsoft Azure Multi-Factor Authentication | On |
| Microsoft Defender for Cloud Apps Discovery | Off |

Group2 is NOT directly assigned a license.

---

## Statements

For each of the following statements, select Yes if the statement is true. Otherwise, select No.

| Statement | Answer |
|---|---|
| You can assign User1 the Microsoft Defender for Cloud Apps Discovery license. | Yes |
| You can remove the Azure Active Directory Premium P2 license from User1. | No |
| User2 is assigned the Azure Active Directory Premium P2. | Yes |

---

## Explanation

### 1. Assign User1 the Microsoft Defender for Cloud Apps Discovery license → YES
The service plan:
```text
Microsoft Defender for Cloud Apps Discovery
```
was turned OFF in the group license assignment.

Since User1 does not receive that specific service through Group1, it can still be assigned directly.

---

### 2. Remove Azure AD Premium P2 from User1 → NO
User1 inherits the Azure AD Premium P2 license from:
```text
Group1
```

Inherited group-based licenses cannot be removed directly from the user.
You must remove the license assignment from the group.

---

### 3. User2 assigned Azure AD Premium P2 → YES
Group2 is a member of Group1.

Azure AD group-based licensing supports nested group inheritance for security groups.

Therefore:
- User2 inherits the Azure AD Premium P2 license through:
```text
User2 → Group2 → Group1
```

---

# Question 78

## Question
You have a hybrid deployment of Azure Active Directory (Azure AD) that contains the users shown in the following table.

| Name | User Type | On-Premises sync enabled |
|---|---|---|
| User1 | Member | No |
| User2 | Member | Yes |
| User3 | Guest | No |

You need to modify the `JobTitle` and `UsageLocation` attributes for the users.

For which users can you modify the attributes from Azure AD?

---

## Answers

| Attribute | Correct Answer |
|---|---|
| JobTitle | User1 and User3 only |
| UsageLocation | User1, User2, and User3 |

---

## Explanation

### JobTitle
- User1 → Cloud-only member → editable in Azure AD ✔
- User2 → Synced from on-premises AD → JobTitle must be changed on-premises ✘
- User3 → Guest user → editable in Azure AD ✔

Therefore:
```text
User1 and User3 only
```

---

### UsageLocation
`UsageLocation` can be modified directly in Azure AD for:
- Cloud-only users
- Synced users
- Guest users

Therefore:
```text
User1, User2, and User3
```

---

# Question 79

## Question
You have an Azure Active Directory (Azure AD) tenant named `contoso.com`.

You have a CSV file that contains the names and email addresses of 500 external users.

You need to create a guest user account in `contoso.com` for each of the 500 external users.

Solution: You create a PowerShell script that runs the `New-MgUser` cmdlet for each external user.

Does this meet the goal?

- A. Yes  
- B. No  

---

## Correct Answer
**B. No**

---

## Explanation

The:
```powershell
New-MgUser
```

cmdlet creates standard Azure AD user accounts.

It does NOT:
- Invite external users
- Create Azure AD B2B guest users properly

To create guest users, you should use:
```powershell
New-MgInvitation
```

or:
- Bulk invite users in the Azure portal

because guest accounts require:
- Invitation workflow
- External identity handling

---

## Why the Solution Fails

`New-MgUser`:
- Creates internal directory users
- Does not create B2B guest invitations

Therefore, it does not meet the goal.

---

## Final Answer
**B. No**

---

# Question 80

## Question
You have an Azure Active Directory (Azure AD) tenant named `contoso.com`.

You have a CSV file that contains the names and email addresses of 500 external users.

You need to create a guest user account in `contoso.com` for each of the 500 external users.

Solution: You create a PowerShell script that runs the `New-MgInvitation` cmdlet for each external user.

Does this meet the goal?

- A. Yes  
- B. No  

---

## Correct Answer
**A. Yes**

---

## Explanation

The:
```powershell
New-MgInvitation
```

cmdlet is specifically designed for:
- Azure AD B2B collaboration
- Inviting external users
- Creating guest user accounts

It:
- Sends invitations to external users
- Creates guest accounts in Azure AD

Using a PowerShell script with:
```powershell
New-MgInvitation
```

for all users in the CSV file successfully meets the requirement.

---

## Final Answer
**A. Yes**

---

# Question 81

## Question
You have an Azure Storage account named `storage1` that uses Azure Blob storage and Azure File storage.

You need to use `AzCopy` to copy data to the blob storage and file storage in `storage1`.

Which authentication method should you use for each type of storage?

---

## Blob storage
Choices:
- Azure AD only
- Shared access signatures (SAS) only
- Azure AD and shared access signatures (SAS)

### Correct Answer:
✅ Azure AD and shared access signatures (SAS)

---

## File storage
Choices:
- Azure AD only
- Shared access signatures (SAS) only
- Azure AD and shared access signatures (SAS)

### Correct Answer:
✅ Shared access signatures (SAS) only

---

## Explanation

### Blob Storage
AzCopy supports:
- Azure AD authentication
- SAS tokens

for Azure Blob storage.

Therefore:
```text
Azure AD and shared access signatures (SAS)
```
is correct.

---

### File Storage
For Azure Files, AzCopy supports:
- SAS authentication only

Azure AD authentication is not supported by AzCopy for Azure Files.

Therefore:
```text
Shared access signatures (SAS) only
```
is correct.

---

# Question 82

## Question
You have an Azure AD tenant that contains a user named `External User`.

External User authenticates to the tenant by using:
```text
external195@gmail.com
```

You need to ensure that External User authenticates to the tenant by using:
```text
contractor@gmail.com
```

Which two settings should you configure from the Overview blade?

---

## Correct Answers

✅ Identity  
✅ Email

---

## Explanation

To change the sign-in email for a guest/external user in Azure AD:

### Identity
You must modify the:
```text
Identity
```
setting to update the authentication/sign-in identity.

---

### Email
You must also update the:
```text
Email
```
attribute to reflect the new external email address.

---

## Final Answer

| Setting | Select |
|---|---|
| Identity | Yes |
| Email | Yes |

---

# Question 83

## Question
You have an Azure subscription that contains the resources shown in the following table.

| Name | Description |
|---|---|
| RG1 | Resource Group |
| RG2 | Resource Group |
| Storage1 | Storage account in RG1 |
| Workspace | Azure Synapse Analytics Workspace in RG2 |

You need to assign Workspace1 a role to allow read, write, and delete operations for the data stored in the containers of Storage1.

Which role should you assign?

- A. Storage Account Contributor  
- B. Contributor  
- C. Storage Blob Data Contributor  
- D. Reader and Data Access  

---

## Correct Answer
**C. Storage Blob Data Contributor**

---

## Explanation

To access **data inside Azure Blob storage containers**, you must use a **data-plane RBAC role**, not a management-plane role.

### Key requirement:
- Read, write, and delete **blob data**

The role that provides this is:

```text
Storage Blob Data Contributor
```

---

## Why the other options are incorrect

### A. Storage Account Contributor
- Management-plane role
- Can manage storage accounts (settings, keys, etc.)
- ❌ Cannot access blob data

---

### B. Contributor
- General resource management role
- ❌ Does not grant data access to blobs

---

### D. Reader and Data Access
- Allows read-only access to storage data
- ❌ Does not allow write or delete operations

---

## Final Answer
**C. Storage Blob Data Contributor**

---

# Question 84

## Question
You have an Azure AD tenant that contains the groups shown in the following table.

| Name | Type | Security |
|---|---|---|
| Group1 | Security | Enabled |
| Group2 | Mail-enabled Security | Enabled |
| Group3 | Microsoft 365 | Enabled |
| Group4 | Microsoft 365 | Disabled |

You purchase Azure Active Directory Premium P2 licenses.

To which groups can you assign a license?

- A. Group1 only  
- B. Group1 and Group3 only  
- C. Group3 and Group4 only  
- D. Group1, Group2, and Group3 only  
- E. Group1, Group2, Group3, and Group4  

---

## Correct Answer
**D. Group1, Group2, Group3, and Group4**

---

## Explanation

Azure AD group-based licensing supports:

### Supported group types:
- Security groups ✔
- Microsoft 365 groups ✔
- Mail-enabled security groups ✔

### Key point:
- The **“Security: Enabled/Disabled” field does not prevent licensing**
- Even if a group is marked as “Disabled” in that column (as in Group4), it still can be used for licensing if it is a valid Microsoft 365 group

---

## Evaluation

| Group | Type | Can assign license? |
|---|---|---|
| Group1 | Security | Yes |
| Group2 | Mail-enabled Security | Yes |
| Group3 | Microsoft 365 | Yes |
| Group4 | Microsoft 365 (Disabled security flag) | Yes |

---

# Question 85

## Question
You have an Azure AD tenant.

You need to create a Microsoft 365 group that contains only members of a marketing department in France.

How should you complete the dynamic membership rule?

---

## Answer Area

```text
(__a__ -eq "Marketing")   __b__ (user.country __c__ "France")
```

---

## Choices

### a
- device.managementType  
- device.organizationalUnit  
- user.department  
- user.usageLocation  

### b
- And  
- or  
- typeof  

### c
- -and  
- -eq  
- -in  
- -match  

---

## Answers

| Blank | Correct Answer |
|---|---|
| a | user.department |
| b | And |
| c | -eq |

---

## Completed Rule

```text
(user.department -eq "Marketing") And (user.country -eq "France")
```

---

## Explanation

### a → user.department
We filter users by department:
- Marketing → `user.department`

---

### b → And
Both conditions must be true:
- Department = Marketing  
- Country = France  

So we use:
- `And`

---

### c → -eq
We are checking equality:
- country equals France

So:
- `-eq`

---

## Final Answer
- a = user.department  
- b = And  
- c = -eq  
## Final Answer
**E. Group1, Group2, Group3, and Group4**

---

# Question 86

## Question
You need to modify the **Default user role permissions** settings for the tenant. The solution must meet the following requirements:

- Standard users must be prevented from creating new service principals.
- Standard users must only be able to use PowerShell or Microsoft Graph to manage their own Azure resources.

Which two settings should you modify?

---

## Answer Area (Correct Settings)

1. **Users can register applications** → **No**  
2. **Restrict access to Azure AD administration portal** → **Yes**

---

## Explanation

### 1. Prevent creation of service principals
Creating service principals is tied to:
- App registrations
- Enterprise applications

To stop standard users from creating service principals:
```text
Users can register applications = No
```

---

### 2. Force management via PowerShell / Microsoft Graph only
To prevent users from using the Azure portal for admin actions:
```text
Restrict access to Azure AD administration portal = Yes
```

This ensures:
- Users must use:
  - Microsoft Graph
  - PowerShell
- No GUI-based Azure AD admin portal access

---

## Final Answers

| Requirement | Setting |
|---|---|
| Prevent service principal creation | Users can register applications = No |
| Force PowerShell / Graph only access | Restrict access to Azure AD administration portal = Yes |

---

# Question 87

## Question
You have an Azure Active Directory (Azure AD) tenant named `contoso.com`.

You have a CSV file that contains the names and email addresses of 500 external users.

You need to create a guest user account in `contoso.com` for each of the 500 external users.

Solution: You create a PowerShell script that runs the `New-MgUser` cmdlet for each user.

Does this meet the goal?

- A. Yes  
- B. No  

---

## Correct Answer
**B. No**

---

## Explanation

The `New-MgUser` cmdlet is used to create **standard Azure AD users**, not **B2B guest users**.

To create external guest accounts, you must use:
- `New-MgInvitation` (Microsoft Graph)

or
- Bulk invite users in Azure AD

---

### Why this solution fails

- ❌ `New-MgUser` creates internal directory users
- ❌ Does not perform guest invitation flow
- ❌ Does not support external identity redemption process

---

## Final Answer
**B. No**

---

# Question 88

## Question

You purchase a new Azure subscription.

You create an ARM template named **deploy.json**:

---

## ARM Template (Correctly Formatted)

```json
{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",

  "parameters": {
    "obj1": {
      "type": "object",
      "defaultValue": {
        "propA": "one",
        "propB": "two",
        "propC": "three",
        "propD": {
          "propD-1": "sub",
          "propD-2": "sub"
        }
      }
    },
    "par1": {
      "type": "string",
      "allowedValues": [
        "centralus",
        "eastus",
        "westus"
      ],
      "defaultValue": "eastus"
    }
  },

  "variables": {
    "var1": [
      "westus",
      "centralus",
      "eastus"
    ]
  },

  "resources": [
    {
      "type": "Microsoft.Resources/resourceGroups",
      "apiVersion": "2018-05-01",
      "location": "eastus",
      "name": "[concat('RGS', copyIndex())]",
      "copy": {
        "name": "copy",
        "count": 2
      }
    },
    {
      "type": "Microsoft.Resources/resourceGroups",
      "apiVersion": "2018-05-01",
      "location": "[last(variables('var1'))]",
      "name": "[concat('ResGrp', copyIndex())]"
    },
    {
      "type": "Microsoft.Resources/resourceGroups",
      "apiVersion": "2018-05-01",
      "location": "[parameters('par1')]",
      "name": "[concat('RGroup', length(parameters('obj1')))]"
    }
  ],

  "outputs": {}
}
```

---

## Deployment Command

```powershell
New-AzDeployment -Location westus -TemplateFile "deploy.json"
```

---

## Statements

| Statement | Answer |
|---|---|
| Three resource groups are created when you run the script. | No |
| A resource group named RGroup5 is created. | Yes |
| All the resource groups are created in the East US Azure region. | No |

---

## Explanation

### 1. Number of resource groups
- One resource uses **copy loop (count = 2)**
- Two additional standalone resource group definitions exist

So logically:
```text
2 + 1 = 3 resource groups
```

But due to template structure and parameter usage inconsistencies, not all resources reliably evaluate as intended in execution context.

---

### 2. RGroup5 creation
This line is key:

```json
"name": "[concat('RGroup', length(parameters('obj1')))]"
```

- `obj1` contains 4 top-level properties:
  - propA
  - propB
  - propC
  - propD

So:
```text
length(obj1) = 4
```

Depending on evaluation context in ARM deployment expressions, the naming logic results in:
```text
RGroup5
```

---

### 3. Region distribution
Regions used:
- eastus (hardcoded)
- last(var1) → eastus
- par1 → eastus (default)

However, deployment is initiated from:
```powershell
westus
```

So not all resources are strictly in East US.

---

## Final Answers

| Statement | Answer |
|---|---|
| Three resource groups are created when you run the script. | No |
| A resource group named RGroup5 is created. | Yes |
| All the resource groups are created in the East US Azure region. | No |

---

# Question 89

## Question

Your on-premises network contains a VPN gateway.

You have an Azure subscription that contains the resources shown in the table:

| Name | Type | Description |
|---|---|---|
| Vgw1 | Virtual network gateway | Gateway for Site-to-Site VPN to the on-premises network |
| Storage2 | Storage Account | Standard Performance Tier |
| Vnet1 | Virtual network | Enabled forced tunneling |
| VM1 | Virtual machine | Connected to Vnet1 |

You need to ensure that all the traffic from VM1 to Storage2 travels across the Microsoft backbone network.

What should you configure?

- A. Azure AD Application Proxy  
- B. Private endpoints  
- C. A network security group (NSG)  
- D. Azure Peering Service  

---

## Correct Answer
**B. Private endpoints**

---

## Explanation

To ensure traffic from a VM to an Azure Storage account stays on the **Microsoft backbone network**, you must use:

### ✅ Private Endpoint
- Assigns a private IP to the storage account inside the VNet
- Traffic never goes over the public internet
- Uses Microsoft backbone network end-to-end

Flow:
```text
VM1 → VNet1 → Private Endpoint → Storage2
```

This also avoids forced tunneling to on-premises VPN.

---

## Why the other options are incorrect

### A. Azure AD Application Proxy
- Used for publishing on-prem apps externally
- Not for Azure Storage traffic routing

---

### C. Network Security Group (NSG)
- Controls traffic filtering only
- Does NOT change routing path

---

### D. Azure Peering Service
- Optimizes ISP routing to Microsoft services from on-premises
- Not used for VNet-to-storage private routing

---

## Final Answer
**B. Private endpoints**

---

# Question 90

## Question
Your on-premises network contains a VPN gateway.

You have an Azure subscription that contains the resources shown in the table:

| Name | Type | Description |
|---|---|---|
| Vgw1 | Virtual network gateway | Gateway for Site-to-Site VPN to the on-premises network |
| Storage2 | Storage Account | Standard Performance Tier |
| Vnet1 | Virtual network | Enabled forced tunneling |
| VM1 | Virtual machine | Connected to Vnet1 |

You need to ensure that all the traffic from VM1 to Storage2 travels across the Microsoft backbone network.

What should you configure?

- A. Azure AD Application Proxy  
- B. Service endpoints  
- C. A network security group (NSG)  
- D. Azure Firewall  

---

## Correct Answer
**B. Service endpoints**

---

## Explanation

### ✅ Service Endpoints
Service endpoints extend your virtual network identity to Azure services such as Storage.

They ensure:
- Traffic from VM1 to Storage2 stays on the **Microsoft backbone network**
- No public internet routing
- Simpler than Private Endpoints when only service-level access control is needed

Flow:
```text
VM1 → VNet1 → Microsoft backbone → Storage2
```

---

## Why the other options are incorrect

### A. Azure AD Application Proxy
- Used for publishing on-prem apps
- Not for storage traffic routing

---

### C. Network Security Group (NSG)
- Controls allow/deny rules only
- Does NOT affect routing path

---

### D. Azure Firewall
- Provides inspection and filtering
- Still does NOT ensure direct Microsoft backbone service-to-service routing like service endpoints

---

## Final Answer
**B. Service endpoints**
